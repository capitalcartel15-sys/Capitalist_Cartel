# Used only for production builds (`npm run build`). Local `npm run dev` ignores
# this and uses the Vite proxy instead. The /api suffix is added automatically.
VITE_API_URL=https://capitalist-cartel.onrender.com
