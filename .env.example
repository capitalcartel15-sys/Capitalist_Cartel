# Frontend (Vite) environment — optional.
# In development the Vite proxy forwards "/api" to the backend, so this can be left unset.
# Set it for production builds where the API lives on a different origin.
VITE_API_URL=https://capitalist-cartel.onrender.com
